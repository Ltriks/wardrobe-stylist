node_modules/
.next/
dist/
coverage/
*.log
.DS_Store
tsconfig.tsbuildinfo
data/

# Local env and secrets (never commit)
.env
.env.local
.env.*.local

# Python virtualenvs used for local tooling (e.g. rembg)
.venv-rembg/

# Local SQLite database files
*.db
prisma/*.db

# User-generated uploads (runtime creates subfolders via `ensureWardrobeAssetDirs`)
public/uploads/**
!public/uploads/.gitkeep
